CREATE TABLE vehicle(
	vehicleId INT NOT NULL,
      make VARCHAR(64),
  	model VARCHAR(128),
  	derivative VARCHAR(255),
);

CREATE TABLE vehicleeeeee(
	vehicleId INT NOT NULL,
      make VARCHAR(64),
  	model VARCHAR(128),
  	derivative VARCHAR(255),
);
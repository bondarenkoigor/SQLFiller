CREATE TABLE vehicle(
	vehicleId INT NOT NULL,
      make VARCHAR(25),
  	model VARCHAR(25),
  	derivative VARCHAR(25),
	timetest TIME
);

CREATE TABLE vehicleeeeee(
	vehicleId INT NOT NULL,
      make VARCHAR(25),
  	model VARCHAR(25),
  	derivative VARCHAR(25),
	datetest DATETIME
);